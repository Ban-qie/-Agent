# V3 TODO：商品、状态、支付与结果交付

进入条件：V2-22 通过。执行顺序按编号；02—10、11—13、14—16 是三个独立业务模块，各模块单独验收后才启用下一模块。现有原始目录只有订单、明细、客户三张解压 CSV，不能假设商品/支付表已经验收。当前 `olist-v2.zip` 可先只读检查成员，避免重复下载。

## V3-01｜冻结本阶段业务需求【必做】

- [ ] 输入：V2 契约及一个真实演示/使用任务。输出 `docs/specs/V3-scope.md`，逐项记录类别排行、商品标识、销售结构、状态、支付、导出/收藏/异常是否启用及理由。建议先类别分析，再状态，再支付；有新需求才增加后面的可选模块。
- 验收：每个启用模块都有一句用户问题、所需表、指标分母、预期 UI 和失败反例。没有数据/需求的模块记录 N/A，不为了表格齐全而全部实现；不新增利润、退款金额、广告归因或预测。

## V3-02｜验收商品与类别数据【推荐，01 启用后必做】

- [ ] 文件：`devtools/prepare_olist.py`、拟新增 `devtools/profile_olist_products.py`、私有 `data/raw/`、`THIRD_PARTY_NOTICES.md`。先检查既有 ZIP 指纹与成员，约束解压路径/大小，只提取实际需要的 products 和 category translation；记录许可、版本、字段、缺失/重复与主键。
- 验收：`product_id` 唯一性、items 外键覆盖、未知类别和翻译缺失有统计；源表不改写，旧快照不覆盖；无法验收时该模块停止。商品 ID 不等于 SKU，页面不能冒称真实店铺 SKU。

## V3-03｜定义商品/类别指标契约【推荐】

- [ ] 依赖 02。文件：V2 的语义注册表、拟新增 `docs/specs/product-metrics.md`。明确类别销售额=sum(该类明细 price)，类别订单数=distinct order_id，类别平均订单商品金额=该类金额/包含该类的订单数；不是整单客单价。商品件数需单独确认每明细行含义后才启用新 metric_id。
- 验收：一个订单跨两类时两个类别订单数各为 1，总体仍为 1；类别金额可加、类别订单数不可直接加。UNKNOWN 保留且有说明；新指标不覆盖 V0/V1 名称与版本。

## V3-04｜创建版本化类别派生表【推荐】

- [ ] 文件：拟新增 E/`product_snapshot.py`、T/`test_product_snapshot.py`。由 items+products 生成 order×category 或 item 粒度快照及 manifest；绑定原始哈希、转换版本、来源关系；始终先按目标粒度聚合。
- 验收：同订单同类多商品、跨类、零价、未知类、缺失 product 的合成 fixture 可手算；类别金额汇总与原 delivered 总额一致；不把旧订单快照原地扩列覆盖。

## V3-05｜类别确定性查询工具【推荐】

- [ ] 文件：拟新增 E/`product_metrics.py`、E/`contracts.py`/worker 对应 dispatch、T 对应测试。只接受规范化维度/指标/期间/类别白名单参数，新增工具注册遵守原权限与预算。
- 验收：类别分组、期间比较、按完整集合排名、未知类分别正确；类别订单数去重；空集合不伪造数据；超过基数上限失败或分页，不截断后冒充全局排名。跨表若采用 SQL，先满足 V2-19。

## V3-06｜类别条件解析与受控选表【推荐】

- [ ] 文件：E/`v1_normalization.py`、语义/关系注册表、E/`v1_runtime.py`、T。让“2018 年 1 月按类别看销售额前 5”绑定 product/category 契约；只有明确类别任务选新增快照。
- 验收：同义类别需可追溯映射；歧义名称澄清；父地区/期间保留，回原父节点不继承类别筛选。没有启用 V2 关系检索时使用确定性审核映射即可。

## V3-07｜类别排行与销售结构页面【推荐】

- [ ] 文件：F、拟新增类别条件/结果组件、FT。类别排行用表格/条形图；销售结构显示份额分母、Top N + 其他和 UNKNOWN；订单数不可加说明随图表出现。
- 验收：份额以完整总体为分母，百分比舍入差异有处理；不能把类别订单数饼图当互斥占比；表/图/解释引用相同 result_id。先做排行，再将结构图拆 07.a，避免一次改多种图协议。

## V3-08｜商品标识查询【推荐】

- [ ] 依赖 03—07。文件：`product_metrics.py`、规范化契约、F、T。在已验收 product_id 上增加精确筛选与商品排行，控制高基数；默认明确 Top N，必要时确定性分页。
- 验收：多明细同商品订单去重；商品 ID 校验/不存在/超长输入有明确结果；不将商品标识映射为未提供的商品名称或 SKU；不以检索相似字符串替代精确 ID。

## V3-09｜地区×类别分析【推荐】

- [ ] 文件：`product_metrics.py`、V2 校验器、F、T。先按 order×category 聚合，再接订单地区；声明二维结果最大行/列和完整性，超限表格分页或拒绝，不能静默取前一段。
- 验收：同订单跨类在每地区类别格去重；金额沿地区/类别汇总守恒；订单数仅对互斥地区可加；选择地区后新分支只继承明确父条件。

## V3-10｜商品模块出口【必做：模块启用时】

- [ ] 原始表独立 SQL/Decimal 核对多商品/多类别/未知类别案例，评测08/09新任务；回归 V1 三指标与 V2 旧节点。
- 输出 `docs/validation/V3-products-summary.json`，记录源数据/许可、派生快照、指标版本、样例、错误/覆盖限制。独立参考代码不得复用生产派生结果作为唯一真值。

## V3-11｜订单状态指标契约【推荐】

- [ ] 文件：语义注册表、拟新增 `docs/specs/order-status-metrics.md`、T。从原始 orders 验收所有状态及时间字段；新增 `orders_by_status`/取消状态订单数等独立指标；取消占比的分母明确为相同期间全部已观测订单。
- 验收：状态快照不是状态事件流；不能当“当月取消动作数”；取消订单可能无明细，不能经 inner join 丢失；V0/V1 三指标仍 delivered。

## V3-12｜状态查询与独立复算【推荐】

- [ ] 文件：拟新增 E/`status_metrics.py`、工具契约与派生表、T。订单数从订单表直接去重，金额类仅在另有可靠定义时启用；未知状态列为未知或阻断导入，不吞掉。
- 验收：无 items 的取消订单仍计入状态数；各互斥状态数量合计=期间总体订单数；支付/销售金额不附会为退款；独立参考与合成边界一致。

## V3-13｜状态页面与模块出口【推荐】

- [ ] 文件：规范化入口、F、FT。页面显示“下单期内订单的当前记录状态分布”，支持明确状态筛选；不得用“退款分析”标题。
- 验收：分母和日期字段可展开核对；状态筛选不会改变旧节点口径；V1 主链路通过；保存状态模块摘要、完整数据前提和不支持范围。

## V3-14｜支付数据与口径验收【推荐】

- [ ] 文件：拟新增 `devtools/profile_olist_payments.py`、语义注册表、许可文档。从已验收 ZIP 只读提取 payments；检查 `(order_id,payment_sequential)`、重复行、payment_type、installments/value 的含义与空值。
- 验收：声明支付记录金额不是财务到账/退款净额；分组数量区分记录数和 distinct 订单数；日期若沿订单 purchase_at，则明确不是支付发生时间。没有支付事件时间不能生成“按付款时间趋势”。

## V3-15｜支付聚合与受控查询【推荐】

- [ ] 文件：拟新增 E/`payment_metrics.py`、对应 snapshot/契约/T。payments 按目标 order 或 order×payment_type 先聚合；如同屏比较销售金额，items 也独立按 order 聚合后连接，绝不直接多对多 Join。
- 验收：2 个明细×2 个支付记录合成例金额不变成四倍；多支付方式订单在各组订单数可重复，明确不可相加；支付和商品金额不相等本身不判数据错误。

## V3-16｜支付方式页面与模块出口【推荐】

- [ ] 文件：F、FT、独立参考脚本。展示支付方式金额/记录数分布，标明分母与源时间；与销售指标并列时用不同标签和单位契约。
- 验收：多支付、零金额、未知方式人工样例；图/表/结果证据一致；旧三指标未变；费用/调用上限延续原图，未新建第二编排。

## V3-17｜导出契约与服务端生成【条件：01 有交付需求】

- [ ] 文件：拟新增 E/`exports.py`、API、T。先实现 CSV + manifest/结构化 JSON：包含 result_id、snapshot/version、条件、生成时间与结果校验哈希；仅导出已有已验证结果，不重新分析。
- 验收：导出数值与页面结果逐项一致；中文、引号、换行正确；CSV 对以 `= + - @` 开头的文本字段有公式注入防护，真正数值按数值类型导出；失败/未验证结果默认不生成正式报表。用户输入不能指定服务器文件路径。

## V3-18｜下载入口与可选图片导出【条件：17 通过】

- [ ] 文件：F、FT、导出路由。按钮绑定当前选中节点，不绑定最后一次响应；下载包含可复核 manifest。若明确要图片，再拆 18.a，基于当前图规范导出并附快照/口径说明。
- 验收：切换分支后导出正确节点；旧快照导出不换新数据；非法 result_id 拒绝；图片结果不截断轴/图例，无法表达完整证据时保留结构化附件。

## V3-19｜分析收藏【条件：有重复使用需求】

- [ ] 文件：拟新增收藏存储契约、API、F、测试。收藏引用已有 node/result，不复制结果或自动再次运行；取消收藏不删除历史。
- 验收：刷新/重启保留收藏，过期引用明确显示；V4 接对象授权时同步覆盖收藏列表与引用；没有需求记录 N/A。

## V3-20｜变化贡献拆解【推荐，先做两张子卡】

- [ ] 20.a 地区贡献：对互斥完整地区集合计算 `ΔS_region`，总差额为 0 时贡献百分比 null，Top N 另列“其他”；参考总体守恒。文件：拟新增 E/`contributions.py`、T；先工具后页面。
- [ ] 20.b 数量/客单价拆解：在两期订单数非零时，采用对称分解 `数量项=(N1-N0)*(A1+A0)/2`、`客单价项=(A1-A0)*(N1+N0)/2`，A 用未舍入 S/N；两项和=总金额差。零分母不做此拆解，显示不可用。
- 验收：增加/减少/总差为零/平均舍入/一侧无订单均有手算例；结论称“算术贡献”，不声称营销/物流等原因。

## V3-21｜简单异常线索【条件：有明确查看需求】

- [ ] 文件：拟新增 E/`anomalies.py`、T、F。冻结一种透明方法，例如日金额 median/MAD，写最小样本数、MAD=0、缺失天、阈值、参考窗口；时间完整性不足时不擅自补零。
- 验收：平稳、单点、缺失日、短序列、全零手算样例；输出观测值/基准/阈值/证据，称统计线索；不称真实业务异常原因或预测。先离线算法，再接 UI 子任务。

## V3-22｜跨模块契约和高基数回归【必做】

- [ ] 文件：V2 校验器/评测集、T、FT。构造跨类+多支付+取消状态组合，不把不同事实表的金额/计数混进一个分母；检查所有启用工具仍有白名单、容量、deadline 和单图预算。
- 验收：不支持组合明确澄清；跨粒度总体正确；高基数不生成误导图；旧节点、错误和空结果展示完整。任何跨表 SQL 必须受 V2-19 保护。

## V3-23｜核心演示、导出一致性与个人贡献记录【必做】

- [ ] 用 V1 原演示加各已启用模块一条任务完成浏览器链路，单列真实模型和故障注入；页面/图表/导出均绑定同一版本。原始源数据独立复算不少于每模块一个多重关联反例。
- 输出新增与继承能力说明、许可证/来源列表、已知限制、样本级评测；不将上游 UI 或认证基础冒称原创。

## V3-24｜固定版本与交接【必做】

- [ ] 依赖22、23及已启用模块出口。运行完整相关回归/类型/构建，固定快照/契约/提交；输出 `docs/ECOMMERCE_V3.md`、`docs/validation/V3-summary.json`，更新 README/HANDOFF。
- 验收：启用/跳过清单与实现相符，数据取得与许可可复核，V0 回退和 V1 演示正常；不得因为可选导出/收藏未启用而伪称已实现或强制加功能。通过后进入 V4-01。
