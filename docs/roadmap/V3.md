# V3：多用户身份、隔离与任务治理（9项必做）

前置V2-S06通过。本文件替代旧商品/状态/支付扩展阶段；那些业务扩展全部转V5。保留一种认证方案、一种主存储、一个顶层图。优先复用项目已有组件；不默认新增Redis、PostgreSQL或第二套编排。

执行前必读：[小步执行协议](LOW_EXECUTION.md)。卡内a/b/c按顺序逐步验收；T/F/R/D/B命令模板、两次失败暂停和账目保护规则统一由该协议定义。主要范围仍以本卡原验收条目为准。

## V3-S01｜冻结上线边界与最小方案【必做】

- [x] 输入V2核心出口；在 `docs/specs/core-release.md` 增加目标OS、访问范围/域名、反向代理、账号来源、人数、同时运行/排队上限、存储和预算。S01a—d设计冻结通过；Windows loopback、受邀密码账号、SQLite，行为验收留S02—S09；详见V3-S01交接。
- 默认建议：单实例、固定只读公共Olist快照、用户私有历史与任务、受控账号，不做开放注册/上传/分享。两测试账号是最低隔离验收，不代表容量目标；并发数须明确后实测。
- 审查现有auth/datalake/workspace依赖；选择一种服务端身份认证方式和一种事务存储。单实例可评估SQLite，需求不满足再选PostgreSQL；不同时实现多套。
- 输出对象/权限和事务边界表：user→workspace→node/result/task，快照为经授权的公共只读资源。说明全项目/全局/用户限额关系。
- 缺账号来源、服务器或域名时集中索取必要信息，同时继续可独立的本地实现；最终真实上线必须等待这些输入，不用猜测配置标成通过。

### 执行细则

- **先读**：`auth/identity.py:init_auth`、`auth/providers/oidc.py`、`workspace_factory.py`、E/`v1_workspace.py`、`budget.py`、`process_limits.py`；这些包含local身份/文件锁/Windows专用边界。
- **a决策表**：在core-release固定identity、store、API、task、budget、deployment六行，每行写选项/理由/待输入/验证方式。默认候选为已有OIDC+单实例事务存储；无可用IdP时仅列受控本地账号替代的额外工作，不悄悄自造token协议。
- **b最小契约**：给S02—09写一个请求/响应样例：既有/analyze、/workspace、/query是否保留；拟新增本人task查询与取消路径/状态码；202与现有同步响应如何兼容。新路径此时仅设计，不开放API。
- **c明确容量**：建议两测试账号、每人1运行、全局2、无队列、超额429；冻结HTTP线程/worker数、请求/模型/工具deadline、lease/轮询间隔和错误恢复阈值。并发2不等于总用户仅2。
- **d缺输入**：集中询问OS/服务器、域名及访问范围、认证来源、允许人数/负载；缺输入时只做无依赖的纯接口/fixture，不能猜一个Linux生产方案。
- **输出/停点**：D检查六类决策不互相矛盾；存储需支持身份+request唯一约束及原子预算。无法证明迁移/信任链安全时按协议提前申请人工调整。通过后V3-S02。

## V3-S02｜可信认证【必做】

- [x] 入口：现有 `auth/`、受限启动配置和认证测试；复用成熟provider/session能力，不手写加密认证协议。S02a—c密码方案离线边界通过，详见V3-S02交接；业务入口仍关闭，浏览器出口留S09。
- 验证身份签名/有效期/适用对象及必要的issuer/audience；会话退出和过期有效；cookie的Secure/HttpOnly/SameSite与TLS、CSRF策略一致。若使用密码，需成熟哈希库、登录限流和安全存储。
- 多用户模式禁止匿名fallback和信任客户端X-Identity-Id/body.owner；模型密钥只在服务端。演示账号通过受控方式创建，不要求自助注册、找回密码或角色管理产品。
- 验收：有效、未登录、伪造、过期、退出后请求分别通过/拒绝；401明确，不泄露凭据。只做前端登录按钮不算完成。

### 执行细则

- **a认证fixture**：先读`auth/identity.py`、选定provider、`auth/gateways/oidc_gateway.py`（登录/回调/退出）、`src/app/oidcConfig.ts`；选OIDC则用tests/backend/auth/test_oidc_provider.py和test_auth.py的fake JWKS/HTTP方式，0真实认证网络。
- **b独立profile**：E/policy增加明确multiuser配置分支，配置无效启动失败或拒绝服务，不能退回anonymous。只放行登录/回调所需精确路由及状态；local模式回归保持，不允许任意/api/auth/*。
- **c会话安全**：backend cookie模式验证state/nonce/PKCE适用项、CSRF、Secure/HttpOnly/SameSite、退出会话失效；token模式明确撤销/有效期行为，不能把清浏览器token称为服务端撤销。
- **案例**：有效A→可读本人身份；无登录/伪签名/过期/错误issuer或audience→401；伪造X-Identity-Id不能成为B；provider不可用受控拒绝，不能fallback。
- **命令**：T显式跑test_auth.py、test_oidc_provider.py、test_flask_session_config.py及拟新增T/test_multiuser_auth.py；未采用OIDC则列实际provider等价测试。V1旧profile测试仍通过。
- **停点/下一步**：S03前不开放分析执行；缺真实IdP仅能标本地fixture通过，S09/部署仍需真实身份。通过实现与离线边界验收后V3-S03。

## V3-S03｜统一对象授权【必做】

- [x] 入口：拟新增E/`authorization.py`、API及T；从服务端记录读取对象owner/workspace，统一检查动作权限。S03a—c策略/fake与stub边界43项通过；真实存储留S04。
- 列表、读取、创建、父节点引用、同ID回放、任务查询/取消、图状态与间接结果引用都验证；ID不能作为文件路径或授权凭据。
- 验收：A猜到B的任何对象ID，读/改/取消/父引用均拒绝且无模型调用；合法本人操作成功；错误不泄露他人结果。暂不需要角色邀请/分享。

### 执行细则

- **a纯授权策略**：拟新增E/authorization.py和T/test_authorization.py；输入verified principal、action、服务端resource_ref，读取repository中的owner/workspace。S04尚未实现时用fake repository，不提前写一套临时文件owner方案。
- **b矩阵**：A可读写A workspace/node/task、只读批准公共snapshot；A对B同类对象、B parent、B幂等回放均拒绝。无对象与未授权对象返回统一约定404或403，列表只返回本人。
- **c接入边界**：用stub route/service验证鉴权先于读取敏感结果、执行或取消；真实repository接入留S04/S05。用户传owner不是可信输入，可直接拒绝未知字段。
- **断言**：本人读取/创建可用；越权时graph/executor/ledger reserve调用均0，响应和日志不含B内容。
- **命令/下一步**：T跑拟新增test_authorization.py及既有test_v1_route；保存对象/动作矩阵到core-release，进入V3-S04。不能以全拒绝作为授权完成。

## V3-S04｜持久化与用户数据隔离【必做】

- [x] 依赖S01/S03；入口E/`v1_workspace.py`、`v1_service.py`、现有workspace工厂和选定存储适配/T，超5文件拆存储契约与接入。S04a—d隔离SQLite/12节点副本迁移通过，原历史及账目未改。
- 按可信user/workspace保存节点、结果、错误和任务；缓存绑定身份与输入指纹，幂等唯一键为身份+workspace+request_id并另存输入指纹校验冲突；原子写、事务或版本校验防覆盖。每用户存储容量有限，不因无限历史拖垮服务。
- 原本机历史保留只读备份，迁移只能由明确指定所有者接收；不把旧文件分配给任意首次登录用户。迁移失败不覆盖旧文件/账目。
- 验收：两用户同名工作区/同request_id互不串数据，并发保存不丢更新；旧V1/V0可按原方式恢复，旧节点不重新调用模型。

### 执行细则

- **a存储schema**：依S01选定实现，拟新增E/workspace_repository.py及T/test_workspace_repository.py；最少保存workspace(owner,id,version)、node(workspace,id,parent,status,conditions,response,版本引用)。复用现有存储接口优先。
- **b真实授权接入**：repository所有查询带服务端owner/workspace范围；节点parent必须属于同workspace。测试A/B同名workspace/相同node ID正常隔离，不能只在列表过滤。
- **c持久化冲突**：事务/版本检查保证两个写者不会静默覆盖；SQLite用事务和有限busy timeout、正确备份方式，不手工复制运行中WAL当备份。网络/模型调用不持有长数据库事务。
- **d旧历史**：先备份、校验原文件hash→只读转换到临时库→明确owner映射→比对节点数/状态/金额/条件→原子启用新路径；不自动迁移给第一个登录者。费用迁移由S07负责，不能在此新建空账目。
- **命令**：T跑test_v1_workspace/test_v1_service及新repository测试；模拟提交前失败、并发写冲突、超容量。新backend未接executor时只验收存储能力，不假装完整分析可用。
- **下一步**：V3-S05。若存储接口/迁移需超过5文件拆a schema、b adapter、c migration分别验收。

## V3-S05｜执行与数据权限闭环【必做】

- [x] 入口E/`executor.py`、工具契约、API、policy和T；将本机local身份假设替换为受权上下文，不只是更改字符串前缀。S05a—c通过，53项回归；隔离fixture入口，无外部开放。
- 所有执行只能访问批准的只读快照；worker只收批准参数/路径，不收模型凭据。其他上游自由代码、任意文件、连接器和管理入口保持关闭。
- 验收：伪造snapshot/workspace、跨用户复用结果/缓存/审计、未知工具、路径穿越拒绝；两合法用户均能查询共享样例快照而各自历史私有。
- 没有上传、导出、证据服务就不新增它们；将来新增时必须纳入同一授权检查。

### 执行细则

- **先读**：E/executor的local前缀和共享execution-audit、API的_workspace/_v1_workspace、workspace_factory与accepted_catalog。
- **a受权上下文**：将可信owner/workspace与允许snapshot传到固定工具；不能简单把startswith('local:')改成接受所有字符串。保持V0 local适配。
- **b执行/缓存隔离**：执行审计、结果缓存及请求冲突检查按owner/workspace/request命名；两用户相同request_id不会命中对方结果或冲突。全局原始快照只能只读共享。
- **c路由接入**：将S02身份、S03授权、S04存储接入多用户API；/query若保留须相同授权/配额/资源限制；若UI不用则明确关闭，不能成为/analyze限制旁路。
- **命令**：T跑test_execution/test_profile/test_v1_route和拟新增test_multiuser_execution.py；两合法用户执行成功，伪snapshot/parent/owner、跨缓存/审计访问拒绝，未知API默认关闭。
- **下一步**：V3-S06；此时仍是隔离开发环境，待任务治理完成才可开放外部访问。

## V3-S06｜任务原子状态与幂等【必做】

- [x] 先定义状态/接口再接存储，复用现有任务实现可行部分；必要时新增E/`task_store.py`及T。不引入第二套业务图。S06a—d通过，双进程1任务/1图，未知费用联动由S07补验。
- 唯一键为身份+workspace+幂等键，输入指纹另存用于冲突比较，不加入唯一键；事务内创建/抢占、运行lease、终态不可覆盖、版本冲突明确；模型调用之前持久化费用预留。
- 同键同输入回放/查询已有任务，同键不同输入冲突；并行提交不得重复执行。队列可不做，满载时明确拒绝；若实现队列必须有上限与超时。
- 验收：按目标进程数并发同键只执行一次；进程崩溃/lease过期后不静默重跑收费步骤。外部模型计费不承诺恰好一次。

### 执行细则

- **a状态纯函数**：拟新增E/task_store.py及T/test_task_store.py，先按共用协议写允许转换/终态映射/lease版本；仅同owner+workspace+request唯一，fingerprint为比较字段。
- **b事务方法**：最少create_or_get、claim、get_authorized、finish_if_owner_version、request_cancel。CAS返回冲突时旧worker不得再写结果；DB调用有界，模型执行不占长事务。
- **c服务接入**：同键同输入running→原task状态/202，终态→原结果；同键不同输入→409。选后台执行时只用有界executor启动现有graph，不能无限新线程；无空位拒绝，不在内存攒无界任务。
- **d竞争反例**：两独立进程通过barrier同时提交同键→只有1持久任务、1图调用；A/B同键→各自任务；旧lease晚返回不能覆盖终态；已有付费reservation但结果未知不能重放。
- **命令**：T跑拟新增test_task_store.py/test_task_service.py及test_v1_service/test_v1_final。费用原子化在S07接入，真实模型仍禁用。
- **下一步**：V3-S07。若任务与节点状态映射不清楚就停，不再建立第二份互相覆盖的“真状态”。

## V3-S07｜限流、并发与费用上限【必做】

- [x] 入口E/`budget.py`、任务入口、选定存储及T；区分登录/提交速率、每用户/全局运行数、等待数、请求大小和费用。S07a—d副本迁移/双进程临界预留通过；真实总账保持247次，不启用真实模型。
- 限额读取与预留原子化；多实例才按实际需要用共享存储限流。每用户和全局预算有上限；沿用247次旧账目及未知费用，不能迁移/增加用户就重置项目10元验证预算。
- 验收：突发、多个用户同时提交、预算临界点均不超额；拒绝返回429/忙碌/预算状态且无新增模型调用；一个用户过载时另一正常用户仍有受控服务机会。
- 真实模型不做压测；压测用可识别stub。Redis为实现选项，不是验收目标。

### 执行细则

- **a迁移计划**：先读UsageLedger.reserve/finish和现有所有行，记录数量/hash/已知金额/未知预留；将历史行只读导入带legacy标记的总账或保留只读历史基座。旧调用不能强行归给任意新用户，也不能重复计入。
- **b原子预留**：在同一事务或可证明原子协议中检查用户+全局额度、写reservation与任务关联；重复请求/回调不重复预留/结算。历史未知费用按原保守规则保留；旧账不再有第二个写者。
- **c限流与容量**：每用户/全局运行slot、请求速率、有限等待分别实现；task失败/取消/进程失效按lease释放slot。不要把模型额度与并发slot混为一项。
- **d反例**：余额仅容1调用时两个进程争抢→只1次reserve/模型dispatch；超限用户A→429/明确费用错误且0调用，B仍能按全局上限执行；重启/新账号不会重置总额。
- **命令**：T跑test_budget和拟新增test_multiuser_budget.py/test_limits.py，使用临时账目测试迁移前后保守已耗金额相等。真实账目迁移前必须备份并在副本演练。
- **停点/下一步**：无法证明双账/多进程一致性立即停；不能删锁/放大10元上限绕过。通过后V3-S08。

## V3-S08｜取消、中断与恢复【必做】

- [x] 入口task store/API、图节点边界/worker及T；超过5文件拆“后端状态”与“接入恢复”。S08a—d后端通过且S09页面映射通过，真实子进程取消/恢复保留费用。
- 取消必须鉴权；每次调用前检查取消/截止，运行工具安全终止，资源释放。模型已发出不能保证立即撤销计费，应停止后续步骤并保留预留/实际用量。
- 网络断开不自动新建任务；查询原ID恢复。进程退出后未知步骤标interrupted，已确认终态不重放；不强制引入LangGraph检查点或自动续跑。
- 验收：启动前、模型中、工具中、结果保存前的取消/崩溃分别有可解释状态；终态取消幂等；他人不能取消；重启不重复收费和丢已完成结果。

### 执行细则

- **a取消竞争**：request_cancel校验owner，运行前/每节点前检查；finish与cancel竞态由单次CAS裁决，已success不改cancelled，cancelled后迟到结果不得成为success。
- **b工具中断**：持有受管worker句柄，安全停止本任务进程树；网络模型调用不可撤销时保留已发出/费用未知，等有界超时后清理，不再执行下一节点。
- **c恢复边界**：隔离进程分别在dispatch前、模型响应前后、工具后、保存前退出；重启读取原task/节点，已终态原样、未知收费步骤interrupted。lease过期不是模型未收费的证明。
- **d状态API**：按S01冻结的GET task和取消路径接入policy精确白名单；GET不启动图/续租或重计费，取消终态幂等返回当前状态。
- **命令**：T跑拟新增test_task_recovery.py/test_task_cancellation.py，以及test_v1_final/test_execution；用event同步故障点，不kill真实服务。断言进程/slot释放、总账保留、后续正常任务成功。
- **下一步**：V3-S09。所有取消/恢复状态需有前端映射，不只增加后台枚举。

## V3-S09｜多用户页面与隔离出口【必做】

- [x] 复用AuthButton/API client/F/FT，显示身份、本人历史、运行状态、取消、401/403/限流提示；先用有上限的轮询，不要求SSE。用户选定密码方案，复用原分析/结果页面与Flask-Session；最终429后端/29前端、类型/构建、真实双本地密码账号浏览器通过。QQ/真实模型/公网不在本轮验收；V3完成即停止，不进入V4。
- 退出/换用户清理可见缓存，刷新/断连读取原任务；仅隐藏按钮不能代替S03授权。长错误文本、后台失败不导致白屏。
- 两个真实测试身份分别完成核心分析，再交叉测试列表/详情/修改/父引用/同ID/查询/取消/缓存；合法操作正常，全部越权反例失败。
- 输出 `docs/validation/V3-multiuser-summary.json`，关联S02—08回归和权限矩阵，完成相关后端/前端/类型/构建。通过后进入V4-S01，未部署前不称公网完成。
### 执行细则

- **a API类型**：先更新src/views/ecommerce.ts或apiClient的202/task/error类型，再在F接本人列表、取消与有限轮询。轮询频率/停止时限按S01，401停止并要求登录，终态停止。
- **b防串屏**：退出/切账号时取消请求或用身份generation丢弃迟到响应；清除内存与持久缓存引用，不允许A的慢响应覆盖B界面。
- **c浏览器矩阵**：两个独立context真实登录A/B，stub仅替换Qwen，不伪造身份；A/B各建任务→互猜ID/parent/取消→A退出B登录→刷新/断连/重启。权限反例必须同时有本人正常正例。
- **命令**：F+实际新增AuthButton/任务UI测试；R显式包含provider、新task/store/authorization测试；B日志记录身份别名/对象ID/状态码而非token。
- **出口**：V3-multiuser-summary记录矩阵、方案/配置版本、数据/账目迁移、测试与费用。fixture账号不冒充真实provider；真实IdP缺失则卡保持未完成。
- **下一步**：固定本地版本后V4-S01，不自动开始V5。


cd "C:\Users\Kobe Bryant\Agent Project"
  .\.venv\Scripts\python.exe -m devtools.v3_accounts create Ban_qie
  .\.venv\Scripts\python.exe -m devtools.run_v3