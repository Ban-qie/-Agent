# V2：核心正确性与异常输入防护（6项必做）

修订：2026-09-18。目标是可真实上线、支持多用户的简历项目。V1-R08已通过；沿用三指标、固定Olist快照、一个顶层LangGraph与V0回退。旧V2-01—22不再执行，历史计划见本地标签 `v0.2.1-v1-closure`。新编号用S避免与旧证据混淆。

“必做”表示必须满足验收，不等于重写已有功能。先查代码和已有证据，满足则复用；只有真实缺口才改代码。总入口的预算、测试与交接规则同样适用。

执行前必读：[小步执行协议](LOW_EXECUTION.md)。卡内a/b/c按顺序逐步验收；T/F/R/D/B命令模板、两次失败暂停和账目保护规则统一由该协议定义。主要范围仍以本卡原验收条目为准。

## V2-S01｜冻结核心范围与缺口【必做】

- [x] 输入：`docs/validation/V1-team-summary.json`、R04—R08私有交接、当前git差异。输出一份 `docs/specs/core-release.md`，后续任务持续更新，不每卡新建长报告。
- 列出必须保留：明确期间三指标、两期比较、日/月趋势、地区拆分、连续追问、澄清、历史分支、图表/表格、保存恢复、失败状态和V0回退。
- 每项写“已有且验证 / 当前缺口 / 不支持”，关联代码/测试。现有语义版本、快照ID、解释事实白名单、预算和只读能力直接复用。
- 验收：核心要求无遗漏；原R06六个失败与修复后20/20分别保留。商品/支付/上传/动态SQL不在首版范围，不为填满技术栈开发它们。
- 主要文件：仅core-release.md；0模型调用。

### 执行细则

- **先读/前置**：V1-R08的release.json与摘要；E/`v1_service.py`、`v1_agents.py`、F的现有结果/条件展示；不重新运行旧付费campaign。
- **a → b → c**：a核对工作树与V1业务manifest，列出改过的文件；b在core-release中建表 `能力 | 当前文件/函数 | 测试/证据 | 缺口 | 后续卡`；c将每个真实缺口归入S02—S05，未支持业务归V5。
- **交付例**：“日趋势：v1_normalization分组切换；test_v1_group_transition；R04 day证据；已验证；无需重构”。预算采用当前账目读取值，247只是上一恢复点，不能写死当实时值。
- **检查**：D。清单须能覆盖README所有核心能力，不以文档陈述替代实际证据；不要求本卡重新跑315项。
- **停点/下一步**：缺私有证据时标证据缺失并先核对代码/离线反例；不可直接勾通过。通过后V2-S02。

## V2-S02｜请求入口与不受信输入【必做】

- [x] 入口：API、E/`contracts.py`、`policy.py`，必要时受限app配置；先检查已有实现。
- 明确请求字节上限、问题长度、JSON结构、字段类型、嵌套/列表数量、日期、节点/请求ID和允许操作；超限在模型/数据库操作前拒绝。错误不回显密钥/内部路径/堆栈。
- 用户文本、历史问题和数据内容不得改变工具白名单、服务端身份、费用额度或文件/网络权限；注入提示不是代码。未知筛选/指标澄清，不悄悄丢弃。
- 验收：空请求、非JSON、null/错误类型、超长Unicode、超大body、未知字段、路径穿越、SQL/脚本/提示注入均为有界错误或澄清；不启动未授权工具、不调用付费模型处理已知非法结构；紧随其后的正常请求仍成功。
- 主要文件不超过5个，必要时拆S02.a契约/S02.b HTTP。添加有实际拒绝意义的反例，不靠模型自我约束。

### 执行细则

- **先读**：API的`analyze/query`、E/`v1_service.analyze_v1`、`contracts.parse_request`、`policy.install_profile`；T/`test_profile.py`、`test_v1_final.py`。
- **a请求契约**：分别列/analyze与/query允许键/类型/长度；沿用8192字节body、2048字节问题、8—64字符ID。冻结空白/未知键/异常Unicode处理，不增加新用户功能。
- **b拒绝路径**：先加反例再最小修复，拒绝发生在读写workspace和模型调用之前；同时检查应用读取的实际字节，不能只信伪造Content-Length。代理层在V4复验。
- **c连续请求**：同一client先发错误body再发一个stub正常分析，确认无500、未留下running节点/费用预留，正常响应不受污染。
- **测试清单**：null、数组、缺键、数字question、未知owner/sql字段、2048/2049字节、多字节文本、8192/8193字节、孤立代理字符、超长parent/../路径、深嵌套。边界内结构合法不等于语义可执行，允许明确澄清。
- **命令/证据**：T跑test_profile、test_v1_final、test_v1_route；新增边界例优先归这些文件。非法结构400/413，原语义澄清保持现有业务状态；精确映射写core-release。通过后V2-S03。

## V2-S03｜指标与条件不漂移【必做】

- [x] 入口：E/`metrics.py`、`v1_normalization.py`、`v1_agents.py`及对应T；复用既有METRIC_VERSION/快照/条件，不强制新建注册中心。
- 保持订单去重、商品金额不含运费、总金额/总订单数的均价、delivered/下单时间、整数分或Decimal、零分母为不可用；不由模型填写数字。
- 明确期间和父条件优先；切换分组清旧排序/Top N，兄弟分支只继承指定父节点；空结果、覆盖外与失败分离。销量歧义仍澄清。
- 验收：独立CSV/可手算fixture核对重复明细、零价/零订单、两期一侧空、日期边界、Top N同分和分组切换。已存在正确测试可复用，只补缺口。
- 不新建动态SQL/AST执行链。固定执行路径保留只读、白名单、行数/耗时/内存限制。

### 执行细则

- **先读**：E/`metrics.py`、`v1_normalization.py`、`v1_agents.QwenTeam.reviewer/interpreter`；T/`test_metrics.py`、`test_v1_group_transition.py`；`devtools/v1_campaign_reference.py`。
- **a参考反例**：优先复用已存在fixture；手算最小例：一订单两件10.00+20.00、另一订单0.00→订单2、金额30.00、均价15.00。再覆盖无订单均价null、基准0时变化率null。
- **b条件继承**：父节点地区排名Top3→SP日趋势，应保留两期、地区SP且清sort/top_n；兄弟分支回根节点不带SP。每天按绝对日期显示，不跨月份伪配对。
- **c模型边界**：严格原始条件不被改写；Reviewer未知decision仍失败；Interpreter未知/重复/非字符串/7个ID拒绝，工具仅执行一次并保留已完成结果。
- **命令**：T跑test_metrics、test_v1_normalization、test_v1_group_transition、test_v1_agents、test_v1_interpreter_output；独立参考只读旧响应，禁止重新POST旧付费任务。
- **停点/下一步**：任何数值差异先定位参考/生产粒度，不改预期凑通过；两次修复失败按共用协议暂停。通过后V2-S04。

## V2-S04｜异常、超时与失败结果【必做】

- [x] 入口：E/`v1_runtime.py`、`v1_service.py`、`executor.py`、`budget.py`与T；超5文件拆为模型边界/执行边界两步。
- 明确模型连接/读取超时、总任务deadline、输出大小、worker资源及终止上限；终态后不得继续下一次调用，失败不自动无限重试。
- 模型无效JSON/未知引用/重复引用/超量引用、provider失败、worker超时/进程退出、保存失败均进入明确失败/中断；已完成结果按真实校验状态保留，不能误标成功。
- 验收：注入上述故障，锁/资源最终释放或过期可恢复；错误不传播为整个服务不可用；下一正常请求成功。预算预留和未知费用保留，重启不能重新获得额度。
- 故障注入只证明程序处理，不能作为真实provider故障率。

### 执行细则

- **先读**：E/`budget.BudgetClient._dispatch`、`executor.run_worker`、`v1_service._run`、`v1_graph.py`；T/test_execution、test_budget、test_v1_final。
- **a模型层**：stub依次模拟连接失败、超时、畸形JSON、超限输出、未知usage；断言无隐式重试，错误code明确，预留不撤掉未知费用。
- **b执行/保存层**：临时worker模拟超时/子进程退出；临时store模拟写失败。断言图不继续解释伪造结果，已完成结果不误标完整成功，恢复不会重新付费。
- **c资源恢复**：故障后再运行正常stub任务；记录活动进程/锁和调用计数。不得直接删除真实锁作为修复。必须定义总deadline如何覆盖阻塞网络调用，不只在节点间检查时间。
- **命令**：T跑test_execution、test_budget、test_v1_runtime、test_v1_agents、test_v1_final，实际新增异常测试一并列出。应用500若是存储不可用必须有脱敏错误、资源回收及服务恢复；非法输入本身不能触发500。
- **停点/下一步**：部分结果未校验通过时不可标“已验证”；原状态无法表达则先写状态映射再改。通过后V2-S05。

## V2-S05｜页面及历史恢复完整性【必做】

- [x] 入口：F/FT、E/`v1_workspace.py`及T；优先复用现有显示和保存协议。
- 页面清楚显示确认条件、指标口径、快照/版本、图表与结果、澄清、超时/失败和可操作下一步；长输入/异常响应不能白屏。图表无法呈现时表格降级。
- 历史节点绑定原条件/结果/版本；刷新恢复不重复分析。旧数据不支持新版本时明确提示且不覆盖文件。只展示已有来源信息，不伪造SQL或“已验证”标记。
- 验收：失败部分结果、未知状态、图表异常、旧节点、长文本、小屏、刷新/断连均可读；表格、图表和解释一致。不新建独立证据数据库/API。

### 执行细则

- **先读**：F的结果渲染、`src/views/ecommerce.ts`错误映射、E/`v1_workspace.read/save_run`；T/test_v1_workspace、test_v1_service、test_v1_route和FT。
- **a响应样本**：使用已有success/clarification/empty/failed+result/interrupted；补未知error code、结果字段缺失、过长message与图表渲染异常。
- **b页面**：先验证FT，再仅修复白屏/错误提示缺口；旧节点缺新字段有明确退化，不捏造指标版本或事实。
- **c恢复**：隔离runtime写入根/子/兄弟节点与失败节点，重启离线服务；选择各节点核对条件和结果，记录0分析POST、0新增usage；禁止修改原本机历史。
- **命令**：T跑test_v1_workspace、test_v1_service、test_v1_route、test_v1_final；前端变更F；B只操作隔离runtime的已保存结果。
- **通过后**：V2-S06。没有前端/存储改动且对应指纹相同可复用原R05，不重建相同证据。

## V2-S06｜核心出口【必做】

- [x] 汇总S01—05与现有参考器的样本级证据；数值/条件/安全/恢复无已知阻塞；固定当前代码指纹和 `docs/validation/V2-core-summary.json`。
- 涉及后端则运行相关回归；前端变化则前端/类型/构建；阶段出口执行完整相关回归。业务代码未改时可引用同指纹V1证据并明确未重跑。
- 不默认重跑20+20付费对照；真实模型仅验证发生变化的路径，按README统一小预算计划执行。所有失败计入报告，不能选成功样本替换失败。
- 通过后进入V3-S01。尚未做多用户或目标部署，此时不能宣称已上线。

### 执行细则

- **a核对**：S01清单逐条附S02—05通过证据，当前业务指纹与证据版本对应；缺证据不能填true。
- **b一次出口检查**：R(stage=V2-S06-attempt1)、必要F及D；后续补丁影响哪些证据就只补那些，不能引用补丁前测试冒充当前。
- **c摘要**：V2-core-summary至少含commit/manifest、core_cases、测试退出码/计数、失败与限制、费用起止/未知usage、V0回退证据、next_task=V3-S01；详细用户结果只放私有目录。
- **付费条件**：未改模型协议默认0次。确需真实复验时先写有限问题清单和绝对上限；旧R06性能仅标历史，不要求刷新P95。
- **停点**：任何核心错误未关闭就停本阶段。通过后固定本地恢复点再V3-S01；不推送/上线。

## 精简原则

旧语义注册表/迁移框架、独立证据服务、额外校验节点、复杂评测平台不再是强制新增模块；其必要的正确性要求已合并到上述验收。关系检索、自动同比/环比等转V5。已实现的安全限制不得删除。
