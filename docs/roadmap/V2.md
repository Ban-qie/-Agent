# V2 TODO：语义可靠性、校验与结果证据

进入条件：V1-R08 通过。默认按编号顺序执行；每卡的输入包含前一张必做卡的输出。全部仍使用一个顶层 LangGraph、原三指标和固定快照。文件简称/通用测试命令见 [README](README.md)。每卡完成后按 [交接模板](HANDOFF.md)记录证据。

## V2-01｜冻结失败集与本阶段边界【必做】

- [ ] 输入：V0/V1 历史失败、本轮解释失败、V1-R06 对照。输出：拟新增 `docs/specs/V2-scope.md`，逐条写问题、期望条件/结果、实际状态、错误层级与证据路径；先保留至少语义歧义、非法事实引用、条件继承、空分母、多表放大合成反例各一例。
- 本卡只整理任务定义，不改模型提示词。验收：每个改造对应真实失败或明确的新增验证需求；区分“线上失败”“离线反例”“尚未支持”；没有失败证明的关系检索仍为未启用。

## V2-02｜扩展指标契约 schema【必做】

- [ ] 文件：E/`metrics.py`、拟新增 E/`semantic_contracts.py`、T/`test_semantic_contracts.py`。定义 metric_id、version、grain、unit/currency、status、time_field、aggregation、denominator、null/rounding、required_sources。保留现有三个 ID 与所有现有数值。
- 验收：旧 `METRIC_VERSION` 能解析为不可变定义；漏分母、未知 grain、重复版本、浮点金额规则被拒绝；币种/时区不明时保留 unknown，不能默认为 BRL/UTC。schema 变化不先连 UI。

## V2-03｜建立术语映射注册表【必做】

- [ ] 文件：拟新增 E/`semantic_registry.py`、T/`test_semantic_registry.py`；读取 V1 的 `METRIC_ALIASES`。注册 alias→metric_id/version，最长术语优先；销量必须保持歧义状态；不添加商品件数实现。
- 验收：“平均订单商品金额”不会命中销售额；“销量”不能绑定订单数；未知版本/同名冲突明确澄清。注册表有显式版本与不可变校验，不依赖模型猜口径。

## V2-04｜时间语义与覆盖策略【必做】

- [ ] 文件：拟新增 E/`time_semantics.py`、T/`test_time_semantics.py`。复用 `Period`，统一左闭右开、源时间无时区转换、观测范围/采集完整性两个独立属性；规范化明确的年月与自定义区间。
- 验收：月底、闰日、跨年、开始≥结束、边界外日期、空观测分别测试；“最新两个完整月”在完整性证据不足时澄清，不按机器日期或最大订单日期偷偷裁剪。

## V2-05｜状态映射版本化【必做】

- [ ] 文件：E/`semantic_registry.py`、拟新增 T/`test_status_semantics.py`。记录源状态 delivered 与当前有效订单范围；其他状态仅列在可发现元数据中，V2 不启用新状态指标。
- 验收：现有默认仍只统计 delivered；“全部订单”“取消订单”不能悄悄落到 delivered；映射版本改变不能修改旧版本对象。

## V2-06｜接入规范化与图状态【必做】

- [ ] 依赖 02—05。文件：E/`v1_normalization.py`、`v1_contracts.py`、`v1_agents.py`、T 对应测试。Planner/Reviewer 读取同一语义版本；图只传已规范化条件和版本引用，不复制多份可修改口径。
- 验收：模型提出另一状态、币种、日期字段或指标版本时拒绝；严格原始条件仍优先；执行器使用的版本等于解释器看到的版本；V1 核心任务数值不变。

## V2-07｜旧节点与契约迁移【必做】

- [ ] 文件：E/`v1_workspace.py`、`v1_service.py`、T/`test_v1_workspace.py`；先定义旧 state_version→新 schema 的纯迁移函数，再接存储。旧记录缺新字段时只绑定可证明的历史版本，不查询新结果补齐。
- 验收：读取旧 V1/V0 fixture 不调用模型或工具；相同旧节点数值和口径不变；未知版本明确“无法按该口径恢复”；迁移失败保留原文件；重复迁移幂等。

## V2-08｜建立独立参考数据与期望集【必做】

- [ ] 文件：拟新增 `tests/fixtures/ecommerce/semantic_cases.json`、`devtools/ecommerce_reference.py`。合成小表可入库；真实 Olist 参考结果仅放私有目录。期望包含明确条件、状态、值、分组键和证据来源。
- 验收：参考器只读原始 CSV/合成表，用独立 Decimal/SQL 逻辑，禁止调用生产 `metric_values` / `summarize_rows`。至少覆盖：重复明细、零价订单、零订单、重复客户关联、两期一侧空、范围边界、Top N 同分；人工可手算合成样例。

## V2-09｜实现数值/粒度校验器【必做】

- [ ] 文件：拟新增 E/`result_validation.py`、T/`test_result_validation.py`。检查结果 schema、整数计数/分、平均值精确分母、总体与互斥分组汇总、重复键、比较百分比零基准；输出校验项目、pass/fail/not_applicable 和版本。
- 验收：注入重复计数/金额少一分/平均数均值化能捕获；合法零价、空结果和不可加的分组不误拦截；Top N 截断不要求其小计等于总体。金额用整数或 Decimal/Fraction，不用浮点近似掩盖差异。

## V2-10｜接入执行后校验节点【必做】

- [ ] 文件：E/`v1_graph.py`、`v1_runtime.py`、`v1_service.py`、T 图测试。校验是确定性节点，位于执行和解释之间；独立参考集用于评测，不在每次生产请求扫描全部原始 CSV。
- 验收：校验失败不进入成功解释；保留原结果为未通过校验的诊断证据，页面不能标成已验证结果；状态/错误码与图预算统一。超过节点预算仍能终止。

## V2-11｜结构化错误分类与前端展示【必做】

- [ ] 文件：E/`contracts.py`、F、FT。区分语义未确认、查询被拒、执行失败、校验不通过、解释失败、空结果、覆盖外；错误字段只含可显示诊断/下一步，不暴露堆栈。
- 验收：同一错误从 API→保存→刷新显示一致；校验失败不会冒充“无数据”；解释失败保留工具结果但标注闭环未完成；一条错误状态一个清晰可操作说明。

## V2-12｜来源证据对象 schema【必做】

- [ ] 文件：拟新增 E/`evidence_contracts.py`、T/`test_evidence_contracts.py`。定义 evidence_id/result_id、run/node/parent、snapshot_id/hash、metric/semantic/query_template_version、规范条件、工具请求、结果哈希、校验摘要、时间与事实 ID。
- 验收：稳定 JSON 序列化和哈希；键顺序不改变结果 ID、条件改变必须改变指纹；凭据字段不允许进入对象；没有 SQL 时记录结构化工具请求/模板依据，不生成假的“实际 SQL”。

## V2-13｜证据持久化与读取接口【必做】

- [ ] 文件：拟新增 E/`evidence_store.py`、API、E/`v1_service.py`、T 路由/存储测试。证据对象在现有工作区内原子保存，节点引用 evidence_id；只读 API 按当前本机限制服务。
- 验收：失败写入不破坏已保存节点；未知/越界 ID 拒绝；重复请求返回同一证据；文件被改动时报告完整性错误。V4 再接用户授权，不先开放通用文件路径。

## V2-14｜结论引用绑定到证据【必做】

- [ ] 文件：E/`v1_agents.py`、E/`v1_service.py`、T Agent 测试。事实字典每条增加稳定事实 ID 与结果字段定位；Interpreter 只选择已验证 ID，保存所引用 evidence_id/事实路径。
- 验收：另一个结果的 ID、旧快照事实、非法数字/因果句无法作为本节点结论；引用映射恢复后可解析到相同值。输出限制不靠模型自我承诺。

## V2-15｜指标说明与证据详情面板【必做】

- [ ] 文件：拟新增 `src/views/ecommerce/EvidencePanel.tsx`、F、FT。显示期间、筛选、三指标口径和版本、数据观测范围/完整性声明、校验状态、事实对应的结果字段；工具请求/转换依据放折叠区。
- 验收：从结论能定位表格中的相关事实；空/失败/旧节点不显示伪造证据；页面不把 SQL AST 等实现细节塞进常规提问流程；长文本与小屏可读。

## V2-16｜关系检索启用决策【必做决策】

- [ ] 输入：01 的失败集和原始多表任务。输出拟新增 `docs/specs/V2-relations-decision.md`：是否出现漏表/错误 Join、参考正确表集、最小方案及预期收益。
- 验收：固定订单粒度路径可正确解决全部当前任务时，17、18 标记 N/A 并记录证据；未来 V3 新表需要可信目录，但不因此自动需要检索。启用时先选确定性目录匹配/一跳扩表，无向量库默认依赖。

## V2-17｜可信表关系目录【条件：16 启用】

- [ ] 文件：拟新增 E/`relations.py`、`tests/fixtures/ecommerce/relations.json`、T/`test_relations.py`。记录 source/table ID、PK/FK、基数、事实粒度、可聚合字段、可访问范围、版本及人工核验来源；模型只能引用目录 ID。
- 验收：orders→items 一对多、orders→payments 一对多的合成例能识别放大风险；未知 FK、粒度冲突或多条歧义边拒绝；检索文本不能扩大表白名单。

## V2-18｜关系候选与一跳扩表【条件：17 通过】

- [ ] 文件：拟新增 E/`relation_selection.py`、E/`v1_runtime.py`、T 对应测试。输入规范指标/维度，检索候选表；最多扩一跳，再经主外键、粒度、白名单和业务规则四项校验。优先使用经审核的先聚合再连接计划。
- 验收：漏表反例改造后成功、无关表不被扩入；多于一跳或无法唯一选择时澄清；记录必要表召回及额外表率，不能只演示一次成功就宣称 RAG 有效。

## V2-19｜SQL AST 与有限修复边界【必做审计，按路径实施】

- [ ] 先审查 E/`worker.py`、`executor.py`：当前是固定指标执行，并不接受模型任意 SQL。形成 `docs/specs/sql-boundary.md`；不为展示 AST 而新增自由 SQL 入口。
- 若已启用 SQL 模板/关系执行：拟新增 E/`sql_policy.py`，选择已有兼容解析器或先固定新依赖；校验单只读语句、AST 节点/表/函数/路径白名单、LIMIT、超时/资源，以及原始条件指纹。修复默认 0 次；有已分类语法失败需求后最多 1 次，修复后重新跑所有检查。
- 验收：写入、CTE 夹写、多语句、外部读文件/网络、未授权表、OR 放宽日期均拒绝；合法查询不过度误拦截。固定路径无 SQL 时记录路径审计通过及动态 SQL 未启用；未来新增 SQL 必须先经过此卡。

## V2-20｜分层评测报告器【必做】

- [ ] 文件：拟新增 `devtools/ecommerce_eval.py`、合成 fixture、T 报告测试。输出样本级条件正确、数值正确、应澄清/实际澄清、应阻断/实际阻断、恢复正确、必要表集合及调用/时间/费用。
- 验收：定义分母：数值正确率=正确可执行样例/应可执行样例；错误捕获率=捕获错误样例/植入错误样例；误拦截率=错误阻断合法样例/合法样例；必要表完整率按有标注多表样例计算，无样例记 N/A。V0/V1/V2 同任务集可重算，不预填改善比例。

## V2-21｜比较入口与可选反馈【推荐/条件】

- [ ] 先复用已有明确两期区间比较，在 F 展示当前期/基准期及边界；只增加可编辑条件确认，不另建计算函数。自动同比/环比仅当 04 的完整性策略可证明两期覆盖时启用，否则保持明确区间查询和说明。
- 纠错反馈有需求时另拆 21.a：保存用户指出的预期条件/证据引用到本地反馈，不立即修改指标或重新调用模型。验收：年份/闰日映射显式，范围不完整时禁止标作完整同比；反馈不自动进入信任目录或 Agent 指令。

## V2-22｜阶段出口与固定版本【必做】

- [ ] 依赖所有必做卡及已启用模块。V1 第 17.7 链路、V0 回退、旧节点版本恢复、时间边界、重复关联、空/零值、非法事实和 SQL 边界回归通过；失败样例形成前后对照和可重算指标。
- 输出 `docs/validation/V2-summary.json`、`docs/ECOMMERCE_V2.md`、变更/依赖/数据/费用摘要与固定提交。明确关系检索/SQL/同比是否启用、哪些 RAG 场景仍不支持。没有全部出口证据不进入 V3。
