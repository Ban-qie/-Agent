# V1 收尾 TODO（必须先完成）

当前状态：R01—R08通过。2026-09-18按用户“核心完整、稳定多用户上线”的目标精简后续路线，下一任务为V2-S01；新顺序见 [入口](README.md)。本页及历史交接中的旧后续任务编号仅作历史依据。初次 [复核报告](V1-review.md) 保留历史失败；当前V1验收见R06-fix、R07和R08交接。

## V1-R01｜当前基线复核【已完成】

- [x] 固定本轮代码指纹，区分 `c3f4abec`、旧 `v0.2.0` 和既有未提交修复；读取原始证据，重新运行相关离线回归、前端、类型与构建。
- 证据：`docs/verification/V1-readiness-20260918-regression.json`、`.local/verification/v1-readiness-frontend.json`、`docs/verification/V1-readiness-6f8d69ec4d/manifest.json`。289/15 通过；完整链路实际失败，不能勾选整个 V1。

## V1-R02｜复现并定位解释阶段失败【诊断完成，根因保留不确定性】

- [x] 依赖 R01。离线33项通过，一次解释步骤真实回放成功、引用合法，未再次复现；历史缺原始输出，具体违规类型仍未知。详见 `docs/verification/V1-R02/handoff.md`，不将诊断完成写成偶发模型失败已修复。
- 文件：E/`v1_agents.py`；T/`test_v1_agents.py`；拟新增仅本地的角色输出诊断。诊断只记录 role、schema 错误种类和脱敏输出，放私有证据；不记录密钥或完整底层请求配置。先离线覆盖每种拒绝，再按剩余预算仅重跑失败步骤一次以捕获真实根因。
- 验收：有效事实可解释；未知/重复/非字符串引用被拒绝；失败保留已验证结果；工具不二次执行、不静默改成 success。若是模型偶发不合规，报告失败率与可操作恢复方式，不通过删除断言掩盖。

## V1-R03｜关闭核心追问与图表语义缺口

- [x] 依赖 R02。分组变化清除旧排序/Top N、同维度保留、本次显式新排名优先；期间/地区/指标正常继承，已写入契约。65项相关测试通过，真实worker验证28/31天完整无伪差额排名。见 `docs/verification/V1-R03/handoff.md`。
- 文件：E/`v1_normalization.py`、`v1_runtime.py`；T/`test_v1_normalization.py`、`test_v1_final.py`。先构造父地区排名带 Top 3 的反例，再测切换日趋势；不要把不同月份的日期直接配对成同一天差额。
- 验收：日趋势按日期排序、未被旧 Top N 截断；同维度追问仍正确继承排名；从原比较父节点新分支不继承 SP；两期绝对日期趋势与地区差额表的语义各自清楚。不能把代码审查疑点直接写成已复现故障。

## V1-R04｜完善验收脚本并跑完真实演示

用户已明确调整medium。R04.a—d已实现并通过，详见 `docs/verification/V1-R04/handoff.md`；下一项R05。

- [x] 依赖 R02、R03。新增campaign接续实现，5项离线测试和8项真实补测通过；历史父节点只读回放，固定累计上限193次，原失败目录保留。当前路径为 `devtools.v1_campaign_run`，旧全量草稿不作为接续入口。
- 演示：两期三指标→地区减少排名→SP 日趋势→回到比较根节点按地区看订单数→月趋势→空结果→覆盖外→澄清及澄清后追问→销量歧义。检查真实表格/图表与条件展示、每个步骤的三角色 trace、重复请求零新增调用。
- 验收：R04新增场景有当前真实 Qwen→LangGraph→工具→页面证据；两个父节点明确标为历史版本回放；失败/澄清各有独立预期；390px 页面无整页横向溢出。旧readiness草稿只走到第3个case，现由campaign入口接续完成，benchmark在R06单独执行。

## V1-R05｜独立复算与当前版本恢复/回退

- [x] 依赖 R04。新增 `devtools.v1_campaign_reference --evidence`，11场景条件/数值独立复算一致，离线真实重启与V0回退零新增调用，重启后追问成功。见 `docs/verification/V1-R05/handoff.md`。
- 验收：总计、分组、平均数、变化率、完整集合 Top N/差额排名一致。独立核对期间和父节点预期，不用响应自身条件当作语义正确性的唯一依据。刷新、实际停服务、禁用模型重启恢复所有节点，零分析 POST/零新调用；再次启用模型后从恢复根节点追问成功。切回 V0 能读取原 V0 历史；保留错误、中断和部分结果测试。

## V1-R06｜V0/V1 同条件性能、费用与行为对照

当前状态：通过。首次20+20对照的V1六个失败仍见 `docs/verification/V1-R06/handoff.md`；修复后20/20与2项定点复验见 `docs/verification/V1-R06-fix/handoff.md`。新旧批次分开统计并披露运行时段偏差；累计247调用，上限调整单独冻结。

- [x] 依赖 R05。固定四个双方支持的显式问题、同快照、Qwen Flash 配置、输出上限、60 秒截止和总费用上限。先计划每版本 20 个非缓存样本，至少 4 种任务；不足 20 时照实记录，不补造数据。V0 上限 2 调用、V1 上限 3 调用是待比较的架构差异，须披露。
- 文件：验收脚本、拟新增 `devtools/v1_readiness_reference.py` 或等价报告器。增加小样本预算模式、两版本交替/交错执行；不能像当前草稿一样仅先跑完 V0 再跑 V1 而不披露时段偏差。所有失败计入分母，错误样本不随意重试或删除。
- 验收：原始样本含问题、条件、数值校验、状态、耗时、调用数、token、估算/未知费用。P95 使用排序后第 `ceil(0.95*n)` 个观测；分别报告所有尝试和成功样本延迟、样本数及失败率。分支/图表/恢复用能力矩阵比较；V0 不支持的追问不能硬记为数值错误。记录差异，不预设 V1 更快/更便宜。

## V1-R07｜完整覆盖、安全与原始证据封口

- [x] 依赖 R06。运行当前相关后端/前端回归、类型/构建；复查模型/执行超时、预算耗尽、解释失败保留结果、只读/白名单、Host/Origin、旧自由执行入口封闭。模型故障可注入，但明确标注，不能冒充真实 provider 行为。
- 文件：T、FT、`devtools/v06_verify.py`、`docs/verification/`。快照/原始 CSV/锁文件指纹、费用账目前缀不变；扫描可发布源码和新增私有日志均无密钥。声明 Olist 是快照观测范围，不把完整自然月份等同于业务采集完整。
- 验收：V1 目标矩阵每项关联当前证据；数值、条件、权限、恢复无未处理阻塞。记录模型不合规失败的真实发生率和恢复边界；没有当前证据的项目不得勾选。

## V1-R08｜固定真实模型版本并允许进入 V2

- [x] 依赖 R07。更新 `docs/V1_MODEL_TEAM.md`、`docs/ECOMMERCE_V1.md` 及新的 `docs/validation/V1-team-summary.json`；旧 `V1-summary.json` 保留为确定性历史或明确标注历史。将本次实际修复和证据摘要固定到可回退提交；保留用户已有修改来源，不移动旧标签或重写历史。
- 验收：新摘要指向真实三 Agent 提交及当前通过的证据；V0 仍可独立运行；运行说明、已知限制、费用和回退相符；桌面开发文档追加最新恢复点。只有此卡通过，才将 README 的“任务一达标出口”勾选，并开始 V2-01。
