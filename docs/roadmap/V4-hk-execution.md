# 香港 V4 上线续作清单

2026-09-23。只执行 V4；每个子步骤验收并保存新 attempt 后继续。不能将上传成功视为生产发布成功。

## 已完成

- [x] 用户已购买香港目标，Ubuntu 24.04.4 LTS，x86_64，2 核，2063228928 bytes 内存，40 GiB 磁盘。
- [x] 用户实测 Docker 29.8.1、Compose v5.5.1、服务 active、无容器；TCP 对外仅 SSH 22，DNS 53 在 loopback。
- [x] 用户报告 Git/rsync 安装及 /home/ubuntu/ecominsight-release 权限 700 完成（未提供版本明细）。
- [x] attempt1 候选包上传哈希与本地一致。
- [x] 本地只读包审计完成；该包判定不可发布。保留 ZIP，不构建、不解压执行。

## 按顺序执行

| 步骤 | 执行者和操作 | 通过标准 |
| --- | --- | --- |
| S01a-1 | Codex：核验仓库、旧包及用户提供的香港环境 | 新审计报告和输入冻结，不修改历史证据；已完成部分记录 |
| S01a-2 | 用户已确认香港 IP/实例名称、域名、访问范围、数据迁移意图和维护窗口；Codex继续固定WSGI/代理版本及资源阈值 | 用户输入已记录；软件版本与完整配置待验收 |
| S01b-1 | Codex：在本地 Linux 隔离环境实现 worker 内存限制、超时、取消和子进程清理 | 正常分析可完成；超限及取消有界退出，无残留子进程；Windows 回归仍通过 |
| S01b-2 | Codex：接入生产 WSGI、PostgreSQL、Redis 和 Qwen 服务端工厂；补应用/代理 Compose 与秘密模板 | 缺配置拒绝启动；不使用原上游 CLI；配置静态检查通过；无真实调用 |
| S01b-3 | Codex：迁移只读源副本到临时 PostgreSQL，比较账号/owner/节点/任务/费用和未知预留 | 源 hash 不变，金额、行数及归属守恒；不得用空库绕过旧费用 |
| S01c/d | Codex：冻结并测试 Host/Origin/代理头/TLS/session/health/readiness | 缺密钥、坏存储、伪造身份和代理头被拒绝，health 零模型调用 |
| S02a-d | Codex：逐项注入模型慢/坏响应、PG锁/连接池、Redis故障、只读/磁盘满、worker退出，检查恢复和日志 | 查询/取消/health 有界；无秘密、未知重复收费或遗留运行任务 |
| S03a-d | Codex：实现备份清单、备份/新实例恢复、A/B恢复、损坏备份拒绝 | 实际恢复、账目守恒、活跃任务 interrupted、零重放；记录RPO/RTO |
| S04a-d | Codex：至少20正常+20非法/重复/越权及故障矩阵，Linux总资源预算下复验 | 无泄漏/重复执行/异常500/OOM；10会话、单分析槽位 |
| S05a-d | Codex：白名单打包、全部构建输入/锁/许可证、文件hash、秘密扫描、同构回滚、演示说明 | 新目录可构建启动；固定发布commit；旧attempt1不覆盖 |
| S06a | Codex准备命令，用户执行或提供明确SSH连接信息后由Codex操作：新版本目录上传、哈希核对、安全注入服务器凭据 | 只对香港目标部署；不在服务器临时修改代码 |
| S06b | 用户在域名控制台配置A记录；Codex准备Caddy/TLS配置、两账号和免费公网验收 | HTTPS、health/readiness、隔离、取消、重启、恢复通过；零真实模型调用 |
| S06c/d | 用户冻结调试预算；Codex执行少量真实Qwen smoke及重启/回滚验收 | 次数/费用在冻结范围内；未知usage保留；不自动重放 |
| S07 | Codex：回归、发布摘要、运行说明、证据索引和本地标签 | 所有前置卡通过；停止，不进入V5或自动推送 |

## 已确认输入（2026-09-23 补充）

香港公网 IP 为 43.132.124.152，实例 ecominsight-hk-prod，2026-10-23 到期，20 Mbps，512 GB/月。ecominsight.cn 已购买实名，唯一入口 https://ecominsight.cn，仅受邀账号。只迁移 V3 多用户账号和历史工作区；旧 V1/V2 单用户历史不导入线上，原文件仍保留。全部旧账目及未知费用继续守恒。Olist 已获用户授权用于非商业汇总演示；官方数据集 v2、CC BY-NC-SA 4.0、快照 hash 和页面署名已核验。维护窗口30分钟，用户负责云控制台恢复。见 input-freeze-attempt4.json 和 `docs/verification/V4-S01/olist-license-attribution-attempt1.json`。

以下为此前收集清单，已获答复，不再重复询问：

1. 香港公网 IPv4、实例 ID/名称、到期日、带宽和月流量包。公网 IP 可以提供；不要提供密码、私钥或 API Key。
2. ecominsight.cn 是否已购买并实名通过；是否以 https://ecominsight.cn 为唯一入口，公网只允许管理员创建的受邀账号登录。
3. 香港网站是否需要迁移本机账号/历史工作区，还是创建新账号/新工作区。无论选择哪种，旧账目和未知费用都保留；旧数据不会自动公开。确认现有 Olist 只读快照可用于该网站。
4. 维护窗口：建议首次上线失败可停机30分钟、用户负责服务器控制台恢复；服务器内受限目录保存备份，后续另安排离机副本。需要用户明确可接受的停机时间及恢复负责人。

当前服务器无需新命令，保持无应用容器。执行入口为 V4-S01a，不能跳过 Linux 平台与账目验证进入 S05/S06。

## Linux 适配进度

- [x] 记录上述目标与数据使用授权。
- [x] 实现 Linux cgroup v2 总内存/PID上限检查、worker地址空间限制及独立进程组清理。
- [x] 编写标准库探针，覆盖正常、超内存、超时与子进程清理。
- [x] Linux基础探针5项通过；真实Parquet/model stub相关测试25项通过，Windows对应25项通过。
- [x] V3真实数据库的只读副本迁移到本地隔离PostgreSQL schema，8张表逐行相等、调试账目原始字节相等、源hash不变。
- [x] 用户明确旧V1/V2单用户历史不迁移；原本地历史、锁和证据继续保留。
- [x] 完成V3迁移反例和本地生产切换；远程生产验收保留到S06。

原Docker Hub网络阻塞已由用户启用代理解决，linux-image-attempt1.log保留。Linux镜像首次漏装pandas的失败也保留，补齐现有版本后同组测试通过。证据：linux-limits-probe-attempt2.log、linux-workers-attempt2.log、linux-adaptation-windows-attempt3.log。

## 全部待完成小任务（以此清单更新，不将局部通过写成整卡通过）

### S01 生产入口、迁移和边界

- [x] 固定Gunicorn 23.0.0和Caddy 2.10.2镜像摘要、线程/超时/代理跳数及资源预算；配置解析通过。完整生产依赖锁和许可证清单留S05。
- [x] 生产工厂连接PostgreSQL、Redis、快照、Qwen；拒绝缺密钥/无账号/错误资源限制配置。
- [x] 应用与代理Compose、只读挂载、持久卷、健康检查、CPU/内存/PID/连接/日志上限。
- [x] 生产端口仅80/443开放，冻结Host/Origin及单跳可信代理来源。
- [x] 旧V1/V2单用户历史排除在线上迁移范围外；不删除、不重新归属、不覆盖V3工作区。
- [x] 迁移反例：重复目标、未知schema、活跃任务、损坏输入、事务失败；拒绝覆盖或部分激活。
- [x] 本地切换演练确认旧writer未运行、使用一致性SQLite备份、撤销旧session、核对全量费用及未知usage；远程切换留S06。
- [x] 数据许可来源及快照hash核验；页面提供署名、许可、历史时间范围，仅显示汇总。
- [x] 完整HTTP边界、缺依赖readiness失败、零模型health检查。

### S02 服务故障与恢复

- [ ] 冻结health、查询、取消、恢复时限和合成故障fixture。
- [ ] 模型慢/拒绝连接/坏响应、PG锁/连接池耗尽、Redis不可用/超时。
- [ ] 只读目录、模拟ENOSPC、worker退出、任务槽满；解除故障后新请求恢复。
- [ ] 日志敏感值扫描、轮转上限、无重复dispatch/长期锁/僵尸worker。

### S03 一致性备份与隔离恢复

- [ ] 定义PG事务一致性、快照、账目、schema/代码版本清单及RPO/RTO。
- [ ] 实现backup/restore CLI；拒绝覆盖原库，明确Redis重建与退出登录行为。
- [ ] 实际A/B成功/失败/活跃任务恢复；归属及金额守恒，活跃任务interrupted，零重放。
- [ ] 缺manifest、篡改字节、缺库/快照恢复拒绝，原实例可用。

### S04 完整组合矩阵

- [ ] 冻结20正常、20非法/重复/越权请求及每类故障样本，统计P95和允许拒绝码。
- [ ] 实测跨应用session/限流/任务协调、断连/取消/重启/存储故障/预算反例。
- [ ] Linux完整应用+PG+Redis+代理资源预算复验；10会话、1分析任务，无队列。
- [ ] 对账dispatch/reservation/task数量，零越权、重复执行/计费和终态覆盖。

### S05 可复现发布与回滚

- [ ] 白名单发布manifest、固定commit、构建输入/锁文件/许可证齐全，新目录重建。
- [ ] 发布包及相关历史秘密扫描；排除原始订单、私有账目、凭据和失败证据。
- [ ] 同构环境回滚演练，不让旧代码盲读新schema，保留回滚期间新增费用。
- [ ] 演示步骤、架构/贡献说明、限制及证据索引。

### S06 香港目标验收

- [ ] 发布前盘点、回滚点、30分钟窗口；新版本目录上传并核对hash。
- [ ] 服务器本地安全注入凭据；迁移私有数据不进入公开代码包。
- [ ] DNS A记录到43.132.124.152；Caddy TLS签发、续期和HTTP跳转检查。
- [ ] 免费公网health/readiness、两账号登录/隔离、任务/取消/刷新/重启/备份验证。
- [ ] 长沙实际访问延迟与失败率、香港2GB资源占用及单任务容量复验。
- [ ] 冻结Qwen smoke问题/次数/小预算/绝对累计上限，再做真实调用并保留失败和未知usage。
- [ ] 真实重启不重放、回滚验证、费用/历史不丢失后开放少量受邀用户。

### S07 出口与停止

- [ ] 后端/前端/类型/构建及部署恢复检查，发布与验收版本一致。
- [ ] 发布摘要、运行/故障/预算/恢复说明、receipt、本地提交/标签。
- [ ] 停止于V4，不自动推送或增加V5功能。

### 当前迁移结果的范围

`cutover-rehearsal-4db597d83a2f.json`通过：V3数据库8表按切换策略守恒（1账号/1工作区/1节点/1任务/3条website_usage，旧session由1变0），六个迁移反例均拒绝，247条调试账目和2条未知usage原始字节相等，V1/V2 `local:` 行为0。最新schema已由完整本地生产栈实际启动并readiness通过。用户已决定不迁移旧V1/V2单用户历史，因此“完整线上历史”只指当前V3多用户数据库及全部费用记录。

## 证据

- docs/verification/V4-S01/candidate-audit-attempt1.json：旧候选包静态审计，预期退出1，发现6项发布缺口；非修复失败计数。
- docs/verification/V4-S01/input-freeze-attempt3.json：香港目标部分冻结，来源为用户终端输出。
- devtools/v4_candidate_audit.py：只读审计，不解压、不运行ZIP、不覆盖报告；不是完整秘密扫描或发布验收器。

既有2GB结果仅证明Windows应用与本地Docker依赖下的短时样本容量。Linux、代理、真实公网与Qwen仍待实测，不能据此承诺持续负载或两任务并行。
