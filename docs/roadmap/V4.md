# V4 TODO：多用户、并发与受控上线

进入条件：V3-24 通过。顺序执行，先选择一个目标访问范围和认证方案；完成本地可评审配置与测试后，实际目标部署作为最后步骤。不能把 Flask 本机受限入口直接改成 `0.0.0.0` 就称上线。

## V4-01｜明确目标环境和验收负载【必做】

- [ ] 输出 `docs/specs/V4-target.md`：本机两账号演示/可信局域网/受控公网三者选一，记录 OS、域名/TLS、进程数、存储、账户来源、备份位置、访问者、并发和任务时限。先准备建议，只有实际目标/账户信息缺失时向用户集中询问一次。
- 验收：至少两个可信身份可用于授权反例；目标范围和暂不支持范围明确；没有服务器信息仍能继续 schema/本地反例，不宣称目标部署通过。

## V4-02｜选择最小持久化方案【必做决策】

- [ ] 审查 `workspace_factory.py`、`datalake/`、已有 SQLAlchemy/认证依赖，输出 storage ADR。根据目标进程/并发需求，在现有本地文件加受控锁、SQLite 事务、PostgreSQL 中选择一种；若文件难以实现可靠任务 CAS，优先评估现有事务存储。
- 验收：列出任务/对象/检查点/账目事务边界、版本冲突行为、备份及迁移。Redis 只为已证明的跨进程限流/队列需求引入，不作默认依赖；选定后不同时实现多种数据库。

## V4-03｜可信身份与权限矩阵【必做】

- [ ] 文件：`docs/specs/V4-auth.md`、现有 `auth/identity.py`/provider。优先复用已有 OIDC/provider 接口，核对 token 验证、issuer/audience/expiry 和会话撤销；按 Python 服务实际实现，不迁移 Sa-Token。
- 验收：定义用户、工作区成员、角色、数据/结果/任务 owner；明确读/创建/运行/取消/分享权限；客户端 UUID 或 `X-Identity-Id` 不能作为受信身份。先矩阵与反例，不先改全部路由。

## V4-04｜服务端认证接入【必做】

- [ ] 文件：现有 `auth/`、启动配置、`tests/backend/auth/`。接入03选定的一种 provider；分开本机单用户与受控多用户 profile。凭据只在服务端；cookie/session 属性与目标 TLS 匹配。
- 验收：有效、过期、伪签名、错误 audience、未登录分别通过/拒绝；多用户模式拒绝匿名 fallback 和伪造 header；退出后请求失效。复用上游功能也要本项目实测。

## V4-05｜对象 ID 与授权服务【必做】

- [ ] 文件：拟新增 E/`authorization.py`、T/`test_authorization.py`。定义 `authorize(identity, action, object_ref)`，从服务端查 owner/membership；对象 ID 不直接拼路径。
- 验收：用户 A 不能读/改/取消 B 的 workspace、snapshot、node、result、evidence、export、task；同 ID 不同身份仍拒绝；授权函数不信任 body.owner 或客户端角色。

## V4-06｜工作区/节点存储隔离【必做】

- [ ] 文件：E/`v1_workspace.py`、`workspace_factory.py` 与02的存储适配、T。把固定本机 workspace ID 映射到服务端受权对象；读写均授权，目录解析验证根路径与符号链接边界；保留旧本机 schema 的只读迁移入口。
- 验收：两用户并行创建同名工作区不互见；列表、详情、父节点引用都隔离；跨工作区 parent 拒绝；旧文件不被静默绑定给任意登录者。

## V4-07｜数据与执行授权【必做】

- [ ] 文件：E/`executor.py`、`contracts.py`、`accepted_catalog` 替代适配、T。把 `local:` 判断改为服务端已验证上下文与明确数据授权；区分公共只读样例快照和私有上传快照，不能只换身份前缀。
- 验收：未授权 snapshot ID 即使知道也不能执行；worker 只接收到批准路径与参数，不接密钥；跨用户缓存键/审计键包含所有授权与版本维度；数据白名单不变宽。

## V4-08｜结果/证据/导出授权【必做】

- [ ] 文件：V2 evidence_store、V3 exports、API、T。所有间接引用都重新验证所属工作区/用户；下载 URL 不允许永久匿名绕过授权。
- 验收：A 直接猜 B 的 result/evidence/export ID，列表/读取/下载均失败；旧分享被撤销后下载失效；错误信息不暴露他人对象存在与内容。

## V4-09｜受限路由和 Host/Origin 策略升级【必做】

- [ ] 文件：E/`policy.py`、API、认证中间件、安全测试。现有策略只允许本机模式，须按01明确域名/代理信任列表更新；继续封闭上游自由代码、开放文件、任意连接器等未验收路由。
- 验收：合法站点可用；错误 Host、外部 Origin、伪造代理头、跨站写请求拒绝；受限模式无新增绕过路径。允许非 localhost 不等于允许任意 Origin/任意身份。

## V4-10｜登录/工作区切换 UI【必做】

- [ ] 文件：现有 `src/app/AuthButton.tsx`、apiClient、F、对应 FT。复用认证组件，增加受权工作区列表和当前身份提示；切换用户清理内存/浏览器可见缓存引用。
- 验收：A 退出 B 登录看不到 A 历史、图表、错误、收藏或下载；刷新不误用旧 localStorage 身份；401/403 状态清楚，禁止页面仅隐藏按钮代替服务端授权。

## V4-11｜两用户授权出口【必做】

- [ ] 至少 A/B 两个真实测试身份运行浏览器/API；先创建各自对象，再逐个交叉读取/修改/取消/导出/父引用攻击；保留样本矩阵和状态码。
- 验收：所有对象级反例被拒绝，合法本人操作正常；“全拒绝所以安全”不能算通过。未通过前不做公网部署。

## V4-12｜任务状态与 CAS 契约【必做】

- [ ] 文件：拟新增 E/`task_contracts.py`、T。定义 queued/running/cancel_requested/success/failed/interrupted/cancelled 状态及 version、lease_owner、lease_expiry、idempotency_key、input_fingerprint；映射用户分析节点状态，不让两个系统互相覆盖。
- 验收：明确允许转换、终态不可改、原子 compare-and-swap、旧版本写入冲突；等待澄清不占运行 worker；取消不等于已退款或模型从未调用。

## V4-13｜任务存储与并发幂等【必做】

- [ ] 文件：拟新增 E/`task_store.py`、02选定存储、T。实现幂等键的身份/工作区 scope 与唯一约束，事务内创建/抢占任务；模型调用发生在已持久化 reservation 之后。
- 验收：两个进程同时提交同键只执行一次；同键不同输入返回冲突；版本过期写不覆盖新状态；崩溃重启不会重置预算或重复执行已完成工具。不能把进程内 dict 锁当多进程证明。

## V4-14｜并发上限、速率与费用配额【必做】

- [ ] 文件：E/`budget.py`、拟新增 limiter、T。分开每用户/工作区/全局并发和费用；延用真实累计账目迁移，不补造历史。限额读取及预留需原子；多 worker 实测后决定是否需要 Redis。
- 验收：突发/并发请求不能超额 reservation；限流返回可解释状态且不调用模型；费用未知保留预留；旧 V0/V1 项目预算计入迁移总额，不能每个用户重新获得旧 10 元额度。

## V4-15｜任务执行调度【必做，方案条件化】

- [ ] 依赖12—14。同步方式若已满足目标则只记录容量；有长任务/断连需求时用同一 LangGraph 的 worker 调度，先支持一个队列，不增加第二套业务编排。
- 验收：队列溢出、worker 崩溃、lease 过期、重复抢占有状态；队列重投递不自动重做模型调用。Redis/RQ/Celery 等只有 ADR 选定一种才安装，先写最小接口再接服务。

## V4-16｜取消与安全中断【必做】

- [ ] 文件：task store/API、图节点边界、worker 管理、T。取消请求鉴权；在每次外部调用/工具执行前检查状态，已运行固定 worker 按平台安全停止。
- 验收：队列前取消零调用、模型进行中取消不再启动下一节点、工具中取消释放资源、终态取消幂等；模型可能已收费时明确保留记录；他人不能取消任务。

## V4-17｜断连/重启恢复与检查点【必做】

- [ ] 文件：task store、E/`v1_service.py`、图状态持久化、T。记录每个步骤是否开始/完成/有外部副作用；客户端断连不重新提交分析，读取状态恢复。需要持久化检查点时只接现有顶层图。
- 验收：模型响应前后、工具完成后、结果保存前分别杀进程；已确认步骤安全恢复；未知模型结果进入 interrupted/待明确恢复，不承诺恰好一次外部计费。备份旧节点/账目后迁移。

## V4-18｜任务进度与重连 UI【必做，传输方案可选】

- [ ] 文件：API、F、FT。先选择简单状态轮询；有明显需求再独立增加 SSE。进度显示真实已持久化阶段，不编百分比；相同 run_id 恢复订阅。
- 验收：刷新/断网/重新登录后能读本人任务；超时停止轮询并提示；完成后不再提交分析 POST；他人不能通过订阅枚举任务；SSE 若未实施记 N/A。

## V4-19｜审计与日志脱敏【必做】

- [ ] 文件：已有日志脱敏模块、授权/任务/预算边界、T。审计只记录可信 actor、action、object ID、结果、时间与关联 run；token/密钥/敏感问题正文按策略删减。审计不等同记录模型内部推理。
- 验收：合成密钥/Authorization/cookie/URL 凭据经过异常、日志、导出都不出现；权限拒绝/取消/预算事件可关联；设容量/保留期且归档不丢未结费用。

## V4-20｜备份与恢复演练【必做】

- [ ] 文件：拟新增 `devtools/backup_ecommerce.py` / restore 工具和运维文档，复用02存储的正确备份机制。包含工作区/快照 manifest/结果证据/任务/账目/版本，密钥用独立安全配置恢复。
- 验收：在新隔离目录恢复，校验哈希、成员授权和版本；恢复已完成节点零模型调用，活跃任务按17恢复；缺文件/损坏备份拒绝，不覆盖现存项目；记录可接受的数据损失窗口/恢复时间实测。

## V4-21｜健康检查与生产配置【必做】

- [ ] 文件：受限 app 启动/部署配置、health/readiness 路由、T。选择符合目标 OS 的生产 WSGI server；debug/reloader 关闭；健康检查不调用付费模型，readiness 检查必要存储/快照/配置。
- 验收：缺密钥、库不可达、账目损坏、数据指纹错误有相应状态；配置和日志不泄露密钥；资源/worker 数量与压测一致；依赖锁及第三方许可证完整。

## V4-22｜角色/只读分享/邀请【条件：有协作需求】

- [ ] 先拆22.a角色矩阵、22.b服务端授权、22.c页面/撤销反例。最小 owner/viewer，viewer 是否能发起计费分析必须单独定义；分享绑定对象/期限/范围，默认不公开数据。
- 验收：链接泄露/过期/撤销/移除成员/跨工作区引用不能绕过；邀请收件人与发送渠道未明确授权不发送；可先生成本地可审阅邀请操作。无需求整个模块 N/A。

## V4-23｜隔离压测与故障演练【必做】

- [ ] 在01目标负载下测并发、同键重复、限流、断连、取消、服务重启、存储失败。压力流量默认使用可识别的模型 stub，真实少量 smoke 单列，避免把压测变成无界付费。
- 验收：无跨用户结果、无覆盖终态、无超额 reservation；报告吞吐/排队/端到端 P95/失败率和样本数；stub 耗时不能宣传为真实 Qwen 性能。未满足目标则降低已声明容量或修复后再验收。

## V4-24｜部署包与发布前审查【必做】

- [ ] 基于已固定提交生成可审阅配置、环境变量清单、启动/迁移/回滚脚本；扫精确待发布树和必要历史、许可证、数据分发与密钥，不修改用户历史来抹除风险。
- 验收：源数据/账目/工作区/详细证据不意外进包；按目标说明启动；支持备份后回滚。只有实际写远端/公开服务需要新授权且尚未授权时，完成可审阅包后再询问，不提前停在空计划。

## V4-25｜明确目标环境部署验收【必做】

- [ ] 依赖11、20、23、24且目标访问授权已具备。实际部署，验证 TLS/域名/代理、两账号、原 V1 核心任务、各启用 V3 模块、下载授权、重启/恢复和健康；密钥仅服务端。
- 验收：保留目标环境配置指纹与实际浏览器/服务证据。若只验收可信局域网就只声明局域网；没有真实公网验证不称公网/生产级完成。

## V4-26｜受控上线出口【必做】

- [ ] 发布运行/回退/备份/故障处理指南、`docs/validation/V4-summary.json` 和固定版本；列出访问范围、容量、未支持操作、数据许可、用量监控和审计入口。
- 验收：两个用户的合法操作及越权反例、并发/恢复/取消、分发/密钥检查全部可复核。此处可以结束项目；只有新增真实数据源或后台任务需求才进入 V5。
